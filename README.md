# How to run


```
python app.py
```

## Content Organization

- Each flashcard can be assigned a `subject` and a `chapter`.
- The manage page lets you create and edit those fields directly.
- The upload page tags generated flashcards with the subject and chapter you enter.
- `GET /api/catalog` returns the known subjects and their chapters for UI suggestions.
